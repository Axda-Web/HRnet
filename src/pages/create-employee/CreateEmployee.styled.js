import styled from "@emotion/styled"

const StyledCreateEmployee = styled.main`

padding: 1rem;

    .link {
        display: flex;
        justify-content: center;
    }
`

export default StyledCreateEmployee